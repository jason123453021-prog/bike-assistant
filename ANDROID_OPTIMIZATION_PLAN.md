# Android 單車導航應用優化技術方案

## 概述
本文檔記錄了為「單車助手」應用實施的 Android 平台優化方案，涵蓋背景權限管理、自動暫停優化、車頭朝前導航等核心功能。

---

## 第一部分：Android 背景權限與前台服務

### 核心痛點
- Android 10+ 對背景定位實施嚴格限制
- 螢幕關閉後，GPS 更新率降至每小時幾次甚至完全停滯
- 系統 Doze Mode 會強制殺除應用程式

### 解決方案

#### 1. Foreground Service 實施
- **必須步驟**：
  - 在 `AndroidManifest.xml` 中聲明 `android:foregroundServiceType="location"`
  - 自 Android 14 起，在啟動服務前動態檢查並申請 `FOREGROUND_SERVICE_LOCATION` 權限
  - 啟動服務時顯示持久化通知（Persistent Notification）

#### 2. Fused Location Provider API 整合
- **優先級**：`Priority.PRIORITY_HIGH_ACCURACY`
- **更新間隔**：`setIntervalMillis(1000)` (每 1 秒更新一次)
- **最短位移**：設為 0 公尺（由上層演算法進行篩選）

#### 3. 電池優化提示
- 提示使用者開啟「不限制」電池優化設定
- 避免應用被系統休眠機制強制殺除

---

## 第二部分：自動暫停優化

### 業界痛點分析

#### 1. 都市峽谷與紅綠燈漂移 (GPS Drift)
- 高樓遮擋或多路徑效應導致 GPS 座標跳動
- 系統誤判為「微幅移動」，累積不真實的里程與時間

#### 2. 低速陡坡爬升 (Low-Speed Climbing)
- 極限爬坡時時速可能降至 3-5 km/h
- 樹蔭遮擋導致 GPS 精度變差
- 單純速度判定易誤判為「暫停」

### 核心演算法

#### 1. 滑動窗口與加權移動平均過濾
```
機制：建立固定長度為 6-8 的環形緩衝區，儲存最近秒數的 GPS 時速
優化：採用時效加權平均
  - 最新的點權重佔 40%
  - 其餘依序遞減
效果：平滑 GPS 瞬間雜訊，保留反應靈敏度
```

#### 2. 雙閥值滯後機制 (Dual-Threshold Hysteresis)
```
移動狀態 -> 自動暫停：加權平均時速必須「持續低於」1.5 km/h
自動暫停 -> 恢復移動：加權平均時速必須「瞬間或持續高於」3.0 km/h
狀態緩衝：持續 2 秒滿足條件才正式切換
```

#### 3. 加速度計感測器融合技術 (Sensor Fusion)
```
觸發條件：GPS 精確度下降或時速低於 5.0 km/h
提高加速度計判定權重

震動特徵提取：
- 對三軸加速度數據進行高通濾波
- 消除重力加速度影響
- 計算合振動幅值 (Variance / Energy)

核心邏輯：
- 單車行進中產生持續高頻特徵震動
- 偵測到震動能量 > 閥值，即判定為「移動中」
- 完美解決低速爬坡被誤判定為暫停的 Bug
```

### 狀態機轉換矩陣 (FSM)

| 當前狀態 | 輸入條件 | 目標狀態 | 觸發行為 |
|---------|---------|---------|---------|
| RIDING (移動) | 加權時速 < 1.5 km/h 且 震動感測度 < 閥值 (持續2秒) | AUTO_PAUSED | 暫停計時、記錄暫停點、觸發提示 |
| AUTO_PAUSED | 加權時速 >= 3.0 km/h 或 震動感測度 >= 閥值 | RIDING | 恢復計時、記錄恢復點、觸發提示 |

---

## 第三部分：車頭朝前導航實現

### 核心資料來源

#### 1. GPS 航向 (Course / Bearing)
- **來源**：前後兩個 GPS 座標點計算的移動方向
- **優點**：時速高時極度精準、穩定
- **缺點**：時速過低或靜止時，GPS 飄移導致地圖狂轉

#### 2. 電子羅盤朝向 (Heading)
- **來源**：手機內建磁力計的設備本體朝向
- **優點**：靜止不動時，手機轉到哪，地圖就轉到哪
- **缺點**：容易受電磁干擾，路面震動造成數據抖動

### 速度切換演算法

```
若 當前時速 > 5 km/h：
  地圖目標方位角 = GPS 航向
  
若 當前時速 <= 5 km/h：
  地圖目標方位角 = 電子羅盤朝向
```

### 關鍵優化技巧

1. **低通濾波（Smoothing）**：平滑處理角度，讓地圖轉動更滑順
2. **震動過濾**：羅盤角度變化 < 3-5 度時忽略不更新，防止畫面抖動
3. **手機安裝角度補償**：根據畫面旋轉方向做角度補償

### 導航省電優化

- 時速穩定 > 10 km/h：完全關閉羅盤感測器，只依賴 GPS 航向
- 時速 < 6 km/h：重新啟動羅盤監聽
- 大幅延長長途騎乘時的手機續航力

---

## 第四部分：感測器與功耗管理

### 加速度計功耗管理

#### 採樣率調校
- **設定**：`SensorManager.SENSOR_DELAY_NORMAL` 或 `SENSOR_DELAY_UI`（約 20-60Hz）
- **理由**：完全足以應付單車震動特徵分析
- **避免**：`SENSOR_DELAY_FASTEST` 會造成嚴重 CPU 喚醒耗電

#### 動態休眠策略
```
當 GPS 速度 > 15 km/h：
  暫時解除註冊加速度監聽器
  
當 GPS 速度 < 6 km/h：
  重新開啟加速度監聽
```

### 情感化用戶體驗

#### 狀態切換延遲
- 判定符合自動暫停時，維持 2-3 秒的「預備暫停」狀態
- 確認這期間速度沒有反彈，再正式觸發 UI 變更

#### 多重體感回饋
- 短促震動 (Haptic Feedback)
- 語音提示（例如："自動暫停記錄" / "已恢復騎乘"）
- 讓車手不低頭看螢幕也能完全信任 App 運作狀態

---

## 實施優先級

### 第一階段（必須實施）
1. ✅ Foreground Service 與背景權限
2. ✅ 雙閥值滯後機制
3. ✅ GPS 與羅盤融合

### 第二階段（重要）
1. 加速度計感測器融合
2. 加權移動平均過濾
3. 狀態機完整實施

### 第三階段（優化）
1. 功耗管理與動態休眠
2. 情感化 UX 改進
3. 性能監控與調試

---

## 技術棧

- **位置服務**：Google Play Services Fused Location Provider API
- **感測器**：Android SensorManager (Accelerometer, Magnetometer)
- **地圖**：Leaflet / MapBox / Google Maps SDK
- **狀態管理**：Finite State Machine (FSM)
- **音頻**：Expo Speech / TTS

---

## 後續工作

- [ ] 實施 Foreground Service
- [ ] 實施自動暫停狀態機
- [ ] 集成 GPS 與羅盤融合
- [ ] 實施加速度計感測器融合
- [ ] 功耗監控與優化
- [ ] 端到端測試與驗證
