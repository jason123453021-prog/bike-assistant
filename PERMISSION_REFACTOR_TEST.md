# 權限管理重構 - 端到端測試指南

## 測試目標

驗證以下功能已正確實現：
1. ✅ 應用啟動時不顯示強制權限 Onboarding
2. ✅ 設定頁面顯示 4 個核心權限的狀態
3. ✅ 點擊「前往設定」按鈕能正確跳轉至系統設定
4. ✅ 返回 App 時自動刷新權限狀態
5. ✅ UI 不被系統導航列（海帶條）遮擋

---

## 測試環境

### 推薦設備
- **Android 虛擬按鍵設備** - 系統導航列高度 ~48dp
- **Android 手勢導航設備** - 系統導航列高度 ~72dp
- **Android 隱藏導覽列設備** - 系統導航列高度 = 0
- **iOS 有 Home Indicator 設備** - iPhone X+
- **iOS 無 Home Indicator 設備** - iPhone 8

---

## 測試步驟

### 1. 應用啟動測試

**目標**：驗證應用啟動時不顯示權限 Onboarding

**步驟**：
1. 卸載並重新安裝應用
2. 啟動應用
3. 觀察應用是否直接進入首頁

**預期結果**：
- ✅ 應用直接進入首頁
- ✅ 不顯示任何權限彈窗
- ✅ 應用可正常使用

**失敗排查**：
- 如果顯示權限彈窗，檢查 `app/_layout.tsx` 中是否有舊的 onboarding 邏輯
- 查看 console 日誌是否有錯誤信息

---

### 2. 設定頁面權限顯示測試

**目標**：驗證設定頁面正確顯示權限狀態

**步驟**：
1. 進入應用首頁
2. 點擊設定標籤頁
3. 觀察設定頁面頂部

**預期結果**：
- ✅ 設定頁面頂部顯示「系統權限狀態」卡片
- ✅ 顯示 4 個權限項目：
  - 位置權限
  - 通知權限
  - 懸浮窗權限
  - 電池最佳化白名單
- ✅ 每個權限顯示狀態標籤（綠色=已授與、紅色=未授與）

**失敗排查**：
- 如果卡片未顯示，檢查 `app/(tabs)/settings.tsx` 中是否正確導入和渲染 `SystemPermissionsStatusBlock`
- 查看 console 是否有 TypeScript 或運行時錯誤

---

### 3. 系統設定導航測試

**目標**：驗證點擊「前往設定」按鈕能正確跳轉

**步驟**：
1. 進入設定頁面
2. 找到未授與的權限（紅色狀態）
3. 點擊該權限卡片上的「前往設定」按鈕
4. 觀察是否跳轉至系統設定

**預期結果**：
- ✅ 應用跳轉至系統設定頁面
- ✅ 根據權限類型跳轉至對應設定：
  - 位置權限 → 應用詳情頁面
  - 通知權限 → 應用詳情頁面
  - 懸浮窗權限 → 懸浮窗權限設定
  - 電池最佳化 → 電池最佳化設定或應用詳情頁面

**失敗排查**：
- 如果未能跳轉，檢查 `lib/intent-launcher-improved.ts` 中的 URI scheme
- 查看 console 日誌中的錯誤信息
- 驗證 Android 系統是否支援該 URI scheme

---

### 4. AppState 監聽和自動更新測試

**目標**：驗證返回 App 時自動刷新權限狀態

**步驟**：
1. 進入設定頁面，記錄某個權限的狀態（例如位置權限=未授與）
2. 點擊「前往設定」按鈕跳轉至系統設定
3. 在系統設定中授予該權限
4. 返回應用（按返回鍵或切換應用）
5. 觀察設定頁面中該權限的狀態是否更新

**預期結果**：
- ✅ 返回應用後，權限狀態自動更新
- ✅ 原本紅色（未授與）的權限變為綠色（已授與）
- ✅ 無需手動刷新或重啟應用

**失敗排查**：
- 如果狀態未更新，檢查 `SystemPermissionsStatusBlock` 中的 AppState 監聽邏輯
- 查看 console 中是否有 `[SystemPermissionsStatusBlock] App returned to foreground` 日誌
- 驗證 `PermissionsManager.getAllPermissionStatuses()` 是否正確返回最新狀態

---

### 5. UI 自適應測試（海帶條防遮擋）

**目標**：驗證 UI 元素不被系統導航列遮擋

**步驟**：
1. 進入設定頁面
2. 滑動到頁面底部
3. 觀察「版本號」和其他底部元素是否被系統導航列遮擋

**預期結果**：
- ✅ 所有 UI 元素完全可見
- ✅ 底部元素距離系統導航列有適當的間距
- ✅ 所有按鈕和交互元素都可點擊

**失敗排查**：
- 如果底部被遮擋，檢查 `styles.content` 中的 `paddingBottom` 是否正確
- 驗證 `ScreenContainer` 是否正確使用 `useSafeAreaInsets`
- 在不同 Android 設備上測試（虛擬按鍵、手勢導航、隱藏導覽列）

---

## 快速檢查清單

在提交前，請確保以下項目都已驗證：

### 應用啟動
- [ ] 應用直接進入首頁，不顯示權限 Onboarding
- [ ] 無任何彈窗或模態對話框阻擋用戶

### 設定頁面
- [ ] 權限狀態卡片在頁面頂部顯示
- [ ] 4 個權限項目都正確顯示
- [ ] 狀態標籤顏色正確（綠色/紅色）

### 系統導航
- [ ] 所有「前往設定」按鈕都能點擊
- [ ] 跳轉至系統設定時無錯誤
- [ ] 支援多種 Android 系統版本

### 自動更新
- [ ] 返回 App 時權限狀態自動刷新
- [ ] 無需手動操作或重啟
- [ ] 狀態更新及時（< 1 秒）

### UI 自適應
- [ ] 底部元素不被系統導航列遮擋
- [ ] 在不同設備上表現一致
- [ ] 所有按鈕都可點擊

---

## 常見問題

### Q1: 權限狀態顯示不正確？
**A**: 檢查 `PermissionsManager.getAllPermissionStatuses()` 是否正確實現，確保返回最新的權限狀態。

### Q2: 系統設定跳轉失敗？
**A**: 驗證 `IntentLauncherImproved` 中的 URI scheme 是否正確。不同 Android 系統可能支援不同的 scheme，檢查 console 日誌中的降級方案。

### Q3: 返回 App 時狀態未更新？
**A**: 檢查 AppState 監聽是否正確註冊。查看 console 中是否有 `App returned to foreground` 日誌。

### Q4: 底部元素被遮擋？
**A**: 確保使用 `useSafeAreaInsets()` 動態計算底部邊距。檢查 `paddingBottom: Math.max(insets.bottom, 預設值)` 的模式是否正確應用。

---

## 測試報告模板

```
## 測試報告 - 權限管理重構

**測試日期**: [日期]
**測試設備**: [設備型號和 Android 版本]
**測試人員**: [名字]

### 測試結果

| 測試項目 | 結果 | 備註 |
|---------|------|------|
| 應用啟動 | ✅/❌ | |
| 設定頁面顯示 | ✅/❌ | |
| 系統導航 | ✅/❌ | |
| 自動更新 | ✅/❌ | |
| UI 自適應 | ✅/❌ | |

### 發現的問題

1. [問題描述]
   - 重現步驟: ...
   - 預期結果: ...
   - 實際結果: ...

### 建議

- [建議 1]
- [建議 2]
```

---

## 下一步

所有測試通過後：
1. 提交代碼變更
2. 更新文檔和 README
3. 準備發佈新版本
4. 監控用戶反饋
